[package]
name = "clix-rss-sync"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Sync configured RSS subscriptions into redb and configured destinations."
license.workspace = true

[dependencies]
anyhow.workspace = true
chrono = { workspace = true, features = ["clock"] }
clap.workspace = true
clix-core = { path = "../clix-core" }
clix-rss-api = { path = "../clix-rss-api" }
clix-rss-delivery = { path = "../clix-rss-delivery" }
clix-rss-store = { path = "../clix-rss-store" }
tokio = { workspace = true, features = ["macros", "rt"] }

[dev-dependencies]
tempfile.workspace = true
