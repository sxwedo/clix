[package]
name = "clix-core"
version = "0.1.0"
edition = "2024"
description = "Core utilities and shared UI/config components for clix."
license = "MIT"

[dependencies]
anstyle = "1.0"
anyhow = "1.0"
indicatif = "0.17"
tokio = { version = "1.40", features = ["macros", "rt-multi-thread", "process"] }
